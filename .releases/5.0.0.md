**Breaking Changes**

* Merged `common-js` and `common-node-js` into a single Lerna-managed repository.
* Removed `common-node-js/sms/TwilioProvider` class from `common-node-js` package.
* Removed `common-node-js/Environment` class.
* Removed `common-js/network/RestProvider` class from `common-js` package (and related classes).
* Removed `browserify` section from package.json file.

**Technical Enhancements**

* Updated the [`log4js`](https://github.com/log4js-node/log4js-node) library to the next major version.
* Updated the [`axios`](https://github.com/axios/axios) library to the next major version.
* Updated the [`big.js`](https://github.com/MikeMcl/big.js) library to the next major version.
* Updated the [`date-fns-tz`](https://github.com/marnusw/date-fns-tz) library to the next major version.
* Updated the [`browserify`](https://github.com/browserify/browserify) library to the next major version.
* Updated the [`glob`](https://github.com/isaacs/node-glob) library to the next major version.
* Updated the [`gulp`](https://github.com/gulpjs/gulp) library to the next major version.
* Updated the [`jasmine`](https://github.com/jasmine/jasmine-npm) library to the next major version.
* Updated the [`body-parser`](https://github.com/expressjs/body-parser) library to the next major version.
* Updated the [`express`](https://github.com/expressjs/express) library to the next major version.
* Updated the [`express-handlebars`](https://github.com/express-handlebars/express-handlebars) library to the next major version.
* Updated the [`express-http-proxy`](https://github.com/villadora/express-http-proxy) library to the next major version.
* Updated the [`multer`](https://github.com/expressjs/multer) library to the next major version.
* Updated the [`node-yaml-config`](https://github.com/leafty/node-yaml-config) library to the next major version.
* Updated the [`nodemailer`](https://github.com/nodemailer/nodemailer) library to the next major version.
* Updated the [`socket.io`](https://github.com/socketio/socket.io) library to the next major version.
* Updated the [`uuid`](https://github.com/uuidjs/uuid) library to the next major version.
* Updated other project dependencies.
