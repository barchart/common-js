**Breaking Changes**

* Renamed `SymbolParser.getIsCash` function to `SymbolParser.getIsFutureCash` (no consumers should exist).

**Technical Enhancements**

* Replaced usages of the `hasOwnProperty` function with `Object.hasOwn` function in the @barchart/common-node-js package.