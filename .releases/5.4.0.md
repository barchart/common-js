**New Features**

* Added support for four-character roots in single-stock futures symbols.

**Technical Enhancements**

* Replaced `Object.hasOwnProperty` with `Object.hasOwn` in @barchart/common-node-js package.